clear; clc; close all;

%%  Take inputs
%   Take input audio file and flatten to mono

[files,paths] = uigetfile('.wav','multiselect','on');

for i = 1 : numel


[input,fs] = audioread('source\Angklung_Cleaned\Calabig-Angklung\G5_staccato.wav');
input = sum(input,2)/2; 

n = 2^nextpow2(length(input));

subplot(211);plot(fftshift(abs(fft(input(1:end),n)/n)));
subplot(212);plot(fftshift(abs(fft(input(1 8340:end),n)/(n-18340))));

